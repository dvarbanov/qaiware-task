module.exports = {
  // Nightwatch globals:
  // All the Nightwatch methods that require a timeout will have
  // this global waitForConditionTimeout timeout specified as the default.
  waitForConditionTimeout: 10000,
  launchUrl: 'http://www.phptravels.net/'
};
